<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ca.usherbrooke.fgen.api.mapper.machine_template_page_Mapper">

    <select id="getTemplateMachine" resultType="ca.usherbrooke.fgen.api.business.machine_template_page">
        select
            image_type_m,
            model_type_m,
            manufacturier_type_m

        from projet.machine_template_page
        where id_usager = #{id_usager}
    </select>

    <select id="getImageMachine" resultType="String">
        SELECT
            image_machine
        FROM projet.machine
        WHERE id_machine = #{id_machine}
    </select>

    <select id="getMachineSurfaceTemplate" resultType="ca.usherbrooke.fgen.api.business.machine_template_surface"

    <select id="getMachinesSpecificsAllID" parameterType="String" resultType="Integer">
        select
            projet.machine_inventory_specific.id_machine
        from
            projet.machine_inventory_specific
        JOIN
            projet.usager
        ON
            usager.id_usager = machine_inventory_specific.id_usager
        where
            projet.usager.nom_usager = #{name_usager}
    </select>

    <insert id="createMachineTemplate" parameterType="ca.usherbrooke.fgen.api.business.machine_template">
        INSERT INTO projet.type_de_machine (model_type_m,
                                            manufacturier_type_m)
        VALUES (#{machine_template.model_machine},
                #{machine_template.manufacturer_machine})
        INSERT INTO projet.machine (image_machine,
                                    id_type_m,
                                    id_remplissage)
        VALUES (#{machine_template.machineImage_machine},
                (SELECT MAX(id_type_m) FROM projet.type_de_machine),
                1)

        INSERT INTO projet.usager_x_machines (id_usager,
                                              id_machine)
        VALUES (#{machine_template.id_usager},
            (SELECT MAX(id_machine) FROM projet.machine))
    </insert>
</mapper>
